{
	"name": "when-in-rome",
	"displayName": "When in Rome",
	"description": "AI-Driven Code Stylist that enforces team conventions. After all, when in rome you should do as the Romans do.",
	"author": "Tristan Deane",
	"license": "UNLICENSED",
	"version": "0.0.1",
	"publisher": "alchemized",
	"repository": "https://github.com/software-trizzey/when-in-rome-lsp",
	"pricing": "Trial",
	"categories": [
		"Linters",
		"Other"
	],
	"keywords": [
		"code-stylist copilot rome conventions code-quality"
	],
	"engines": {
		"vscode": "^1.75.0",
		"node": ">=18"
	},
	"activationEvents": [
		"onLanguage:python",
		"onLanguage:javascript",
		"onLanguage:typescript"
	],
	"main": "./client/out/extension",
	"contributes": {
		"configuration": {
			"type": "object",
			"title": "When In Rome Configuration",
			"properties": {
				"whenInRome.notificationInterval": {
					"type": "number",
					"default": 1200000,
					"description": "Interval in milliseconds to control the frequency of test reminder notifications for modified files."
				},
				"whenInRome.trace.server": {
					"scope": "window",
					"type": "string",
					"enum": [
						"off",
						"messages",
						"verbose"
					],
					"default": "on",
					"description": "Traces the communication between VS Code and the language server."
				},
				"whenInRome.isDevMode": {
					"type": "boolean",
					"default": true,
					"description": "Enable/disable development mode."
				},
				"whenInRome.isJavascriptEnabled": {
					"type": "boolean",
					"default": true,
					"description": "Enable/disable JavaScript When in Rome extension."
				},
				"whenInRome.isTypescriptEnabled": {
					"type": "boolean",
					"default": true,
					"description": "Enable/disable TypeScript When in Rome extension."
				},
				"whenInRome.isPythonEnabled": {
					"type": "boolean",
					"default": true,
					"description": "Enable/disable Python When in Rome extension."
				},
				"whenInRome.onlyCheckNewCode": {
					"type": "boolean",
					"default": false,
					"description": "Check only new or modified code."
				}
			}
		}
	},
	"scripts": {
		"vscode:prepublish": "NODE_ENV=production npm run compile",
		"clean": "rimraf ./client/out ./server/out",
		"compile": "tsc -b",
		"package": "vsce package",
		"publish": "vsce publish",
		"dev-publish": "npm run package && code --install-extension when-in-rome-0.0.1.vsix",
		"watch": "tsc -b -w",
		"lint": "eslint ./client/src ./server/src --ext .ts,.tsx",
		"postinstall": "cd client && npm install && cd ../server && npm install && cd ..",
		"test": "sh ./scripts/e2e.sh"
	},
	"devDependencies": {
		"@types/mocha": "^10.0.6",
		"@types/node": "^18.14.6",
		"@typescript-eslint/eslint-plugin": "^7.1.0",
		"@typescript-eslint/parser": "^7.1.0",
		"eslint": "^8.57.0",
		"mocha": "^10.3.0",
		"typescript": "^5.3.3"
	}
}
